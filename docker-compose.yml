services:
  postgres:
    image: bitnami/postgresql:latest
    ports:
      - '5432:5432'
    environment:
      - POSTGRESQL_USER=admin
      - POSTGRESQL_PASSWORD=admin
      - POSTGRESQL_DATABASE=unitasker
    volumes:
      - unitasker_pg_data:/bitnami/postgresql

volumes:
  unitasker_pg_data: